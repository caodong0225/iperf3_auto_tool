# 打包说明

## 打包成单个可执行JAR

项目已配置为生成单个包含所有依赖的可执行JAR文件。

### 打包命令

```bash
mvn clean package
```

### 打包结果

打包完成后，会在 `target` 目录下生成：
- **`auto-iperf3-1.0.jar`** - 这是包含所有依赖的可执行JAR文件（单个文件）

### 运行打包后的JAR

```bash
java -jar target/auto-iperf3-1.0.jar
```

### 注意事项

1. **只需要一个JAR文件**：打包后的 `auto-iperf3-1.0.jar` 包含了所有依赖，可以直接运行，无需其他jar文件。

2. **JDK版本要求**：运行JAR文件需要JDK 17或更高版本。

3. **文件大小**：由于包含了所有依赖（JSch、FlatLaf、Logback等），JAR文件会比较大（通常10-20MB），这是正常的。

4. **如果打包失败**：
   - 确保网络连接正常（需要下载依赖）
   - 检查JDK版本是否为17或更高
   - 运行 `mvn clean` 清理缓存后重试

### 打包配置说明

项目使用 `maven-shade-plugin` 进行打包：
- 将所有依赖打包进单个JAR
- 自动设置主类为 `com.github.jyzxc.autoiperf.Main`
- 处理服务文件合并（如SLF4J的ServiceLoader）
- 排除签名文件，避免冲突

### 分发

打包后的 `auto-iperf3-1.0.jar` 可以直接分发给其他用户使用，只需要：
1. 确保目标机器安装了JDK 17+
2. 运行 `java -jar auto-iperf3-1.0.jar` 即可
